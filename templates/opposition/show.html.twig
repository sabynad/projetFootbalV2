{% extends 'base.html.twig' %}

{% block title %}Opposition{% endblock %}

{% block body %}
    <h1>Opposition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ opposition.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ opposition.date ? opposition.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Score_equipe1</th>
                <td>{{ opposition.scoreEquipe1 }}</td>
            </tr>
            <tr>
                <th>Score_equipe2</th>
                <td>{{ opposition.scoreEquipe2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_opposition_index') }}">back to list</a>

    <a href="{{ path('app_opposition_edit', {'id': opposition.id}) }}">edit</a>

    {{ include('opposition/_delete_form.html.twig') }}
{% endblock %}
